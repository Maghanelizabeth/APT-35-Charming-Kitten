Detects credential spraying and phishing follow-on login attempts.

index=auth OR index=o365 OR index=security
(EventCode=4625 OR EventCode=4624)
| stats count dc(src_ip) as ips dc(user) as users by user
| where ips > 5 AND count > 10
| sort - ips
